{
    "build_phase" : [
        {
            "command" : "exec",
            "exec_base" : [
                "$dep", "mingw", "w64devkit", "bin", "make.exe"
            ],
            "args" : [
                "-C" , "$src\\src", "CC=x86_64-w64-mingw32-gcc", "PLATFORM=PLATFORM_DESKTOP", "PLATFORM_OS=WINDOWS", "RAYLIB_LIBTYPE=SHARED"
            ]
        }
    ]
}
